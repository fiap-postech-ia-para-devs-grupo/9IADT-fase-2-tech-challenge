import pytest


def test_gerar_explicacao_returns_string():
    # TODO: replace with real call once agente.py is implemented
    # from src.llm.agente import gerar_explicacao
    # result = gerar_explicacao("MALIGNO", 0.87, [...])
    # assert isinstance(result, str) and len(result) > 0
    pytest.skip("LLM agent not yet implemented")
