import pytest


def test_fitness_returns_float():
    # TODO: import fitness function from notebook/module once extracted
    # from src.ag.genetic import fitness
    # result = fitness({"n_estimators": 100, "max_depth": 5, ...}, X_train, y_train)
    # assert 0.0 <= result <= 1.0
    pytest.skip("AG module not yet extracted from notebook")
